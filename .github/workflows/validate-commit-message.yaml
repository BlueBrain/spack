name: Commit message validation

on:
  pull_request:

jobs:
  validate-commit-messages:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.ref }}
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Get diff with base branch
        id: changed-files
        run: |
          CHANGED_FILES=$(git diff --name-only 8c3a2a3fe1a7570e84606985b2d902611501a56a...90fc5882b33c13692b8a67d1976982027c491c0b | jq -sR 'split("\n") | map(select(. != "")) | @json' | sed -r 's/^"|"$/\\"/g' | tr -s /)
          echo "changed-files=\"${CHANGED_FILES}\"" >> $GITHUB_OUTPUT


      - name: Show debug info
        run: |
          echo "${{ github.event.pull_request.commits }}"
          echo "${{ github.event.pull_request.base.ref }}"
          echo "${{ github.event.pull_request.head.repo.fork }}"
          echo "${{ steps.changed-files.outputs.changed-files }}"

      - name: Validate commit messages
        id: commit-message-validator
        continue-on-error: true
        run: |
          pip install gitpython
          python ./.github/workflows/validate-commit-message.py --title "${{ github.event.pull_request.title }}" --changed-files "${{ steps.changed-files.outputs.all_changed_and_modified_files }}" --commits "${{ github.event.pull_request.commits }}"

      - name: Show output
        run: |
          echo "Faulty commits: ${{ steps.commit-message-validator.outputs.faulty-commits }}"
          echo "Script failure: ${{ steps.commit-message-validator.outputs.script-failure }}"

      - name: Add comment to PR
        if: ${{ steps.commit-message-validator.outputs.faulty-commits == 'true' }}
        uses: thollander/actions-comment-pull-request@v2
        with:
          filePath: message_issues.txt
          reactions: eyes, confused, rocket

      - name: Fail the action
        if: ${{ steps.commit-message-validator.outputs.faulty-commits == 'true' || steps.commit-message-validator.outputs.script-failure == 'true' }}
        run: |
          echo "Something is not right - failing"
          exit 1
